{ config, lib, pkgs, ... }:

let
  script = pkgs.writeShellApplication {
    name = "prompt-password";
    text = ''
      fuzzel --dmenu \
            --prompt-only "󰌆  " \
            --placeholder "" \
            --password \
            --width 30
    '';
  };
in
{
  home.packages = [
    script
  ];
}